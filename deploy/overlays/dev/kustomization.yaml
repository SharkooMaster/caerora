apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: caerora-dev

resources:
- ../../base

patches:
- path: configmap-patch.yaml
- path: ingress-patch.yaml

# Image tags are bumped by CI (build-deploy workflow) on pushes to `develop`.
images:
- name: caerora-backend
  newName: ghcr.io/sharkoomaster/caerora-backend
  newTag: dev-81cd7ece87a4
- name: caerora-frontend
  newName: ghcr.io/sharkoomaster/caerora-frontend
  newTag: dev-81cd7ece87a4
