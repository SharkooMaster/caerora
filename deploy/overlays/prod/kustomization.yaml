apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: caerora-prod

resources:
- ../../base

patches:
- path: configmap-patch.yaml
- path: ingress-patch.yaml

# Image tags are bumped by CI (build-deploy workflow) on pushes to `main`.
images:
- name: caerora-backend
  newName: ghcr.io/sharkoomaster/caerora-backend
  newTag: prod-7af0f67e40d0
- name: caerora-frontend
  newName: ghcr.io/sharkoomaster/caerora-frontend
  newTag: prod-7af0f67e40d0
